- name : loops
  hosts: local
  connection: local
  become: yes
  tasks:
  - name: install package
    ansible.builtin.package:
      name: "{{ item.name }}"
      state: "{{ item.state }}"
    loop:
    - { name: "mysql", stae: "present"}  
    - { name: "nginx", stae: "absent"}  
    - { name: "zip", stae: "present"}